node_modules
.next

# TypeScript incremental build cache — regenerated by every `tsc`/`next build`,
# so committing it only produces noisy diffs.
tsconfig.tsbuildinfo

# Scratch/inspection artifacts — throwaway HTML dumps, probe scripts and logs.
# `.ev.html` (a saved render used to measure page spacing) was committed by
# accident in e3a3bf5; these keep that from recurring. Anything transient should
# go in .scratch/ rather than a dotfile at the repo root.
.scratch/
.*.html
.*.log
node_modules
